{
	"$schema": "../gen/schemas/mobile-schema.json",
	"identifier": "barcode-scanner",
	"description": "QR pairing: the scanner plugin is only built for mobile targets.",
	"platforms": ["android", "iOS"],
	"windows": ["main"],
	"permissions": ["barcode-scanner:default"]
}
