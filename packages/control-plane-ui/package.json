{
	"name": "@workbench/control-plane-ui",
	"version": "0.1.0",
	"private": true,
	"type": "module",
	"description": "Shared, transport-driven control-plane UI (projects, worktrees, remote session spawn) for the desktop remote-client and the mobile app.",
	"svelte": "./src/index.ts",
	"exports": {
		".": {
			"svelte": "./src/index.ts",
			"default": "./src/index.ts"
		}
	},
	"scripts": {
		"check": "svelte-check --tsconfig ./tsconfig.json",
		"test": "vitest run"
	},
	"dependencies": {
		"@workbench/transport": "workspace:*",
		"@workbench/types": "workspace:*",
		"@workbench/ui": "workspace:*"
	},
	"peerDependencies": {
		"svelte": "^5"
	},
	"devDependencies": {
		"@sveltejs/vite-plugin-svelte": "catalog:",
		"@tsconfig/svelte": "catalog:",
		"svelte": "catalog:",
		"svelte-check": "catalog:",
		"typescript": "catalog:",
		"vitest": "catalog:"
	}
}
