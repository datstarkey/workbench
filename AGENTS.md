# AGENTS

## Package manager

- Use Bun as the package manager for this repository.
- Prefer `bun install` and `bun run <script>`.
- Do not introduce npm/yarn/pnpm lockfiles.

## Common commands

- Install: `bun install`
- Dev: `bun run dev`
- Build: `bun run build`
- Check: `bun run check`
